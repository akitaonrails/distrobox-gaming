_dg_fling_complete() {
  local cur
  cur="${COMP_WORDS[COMP_CWORD]}"
  if [[ ${COMP_CWORD} -eq 1 ]]; then
    COMPREPLY=( $(compgen -W "list keys menu $( {{ dg_steam_trainers_host_launcher | quote }} keys 2>/dev/null )" -- "$cur") )
  fi
}
complete -F _dg_fling_complete dg-fling
