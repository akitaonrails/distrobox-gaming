[Desktop Entry]
Name={{ item.name }} (on {{ dg_box_name }})
Exec=/usr/bin/distrobox-enter -n {{ dg_box_name }} -- {{ dg_box_home }}/bin/{{ item.slug }}-launch
Terminal=false
Type=Application
Icon=dosbox-staging
Comment={{ item.name }} — GOG MS-DOS racing game via dosbox-staging in {{ dg_box_name }}
Categories=Game;
StartupWMClass=dosbox;
