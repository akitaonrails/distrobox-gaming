---
# ogm (omarchy-games-menu) integration — a host-side game launcher that builds
# its collection by scanning the host's ~/.local/share/applications for the
# very desktop entries this repo exports (gaming-*, pc-*, screamer*,
# ridge-racer, ...). See docs/ogm-launcher.md.
#
# Why a separate list instead of ogm_* fields on the existing launcher data:
# the launchers ogm consumes are spread across several data structures —
# dg_emulator_launchers (launchers.yml), dg_pc_racing_games (pc_racing.yml),
# dg_screamer_games (screamer.yml), dg_dkc_games (dkc_recomp.yml) — plus
# one-off templates in roles/desktop_apps/templates/. Threading ogm_* keys
# through all of them would duplicate the same concern across six-plus files;
# one list keyed by desktop stem keeps the ogm contract in a single place and
# references the existing dg_* vars for values (GitHub repos especially)
# instead of restating them.
#
# dg_ogm_games fields (id / desktop_id / category required):
#   desktop_id   desktop file stem in ~/.local/share/applications (no .desktop)
#   id           ogm slug (desktop stem minus the gaming-/pc- prefix)
#   name         optional display-name override; set only where the repo
#                already tracks a clean title (dg_*_games name fields) and the
#                rendered desktop Name= carries a "(on gaming)"-style suffix
#   category     port|decomp|recomp|fangame|wine|arcade|emulator|tool|custom
#   github       owner/repo — ALWAYS derived from an existing dg_* var (the
#                regex below strips any github.com URL form down to
#                owner/repo); never a value invented in this file
#   sgdb_query   SteamGridDB search name — unused so far, the repo does not
#                track SteamGridDB titles anywhere

dg_ogm_enabled: true
dg_ogm_catalog_path: "{{ ansible_env.HOME }}/.config/ogm/catalog.d/distrobox-gaming.toml"

dg_ogm_games:
  # ── Emulator apps ────────────────────────────────────────────────
  - { desktop_id: gaming-es-de, id: es-de, category: tool }
  - { desktop_id: gaming-dolphin, id: dolphin, category: emulator }
  - { desktop_id: gaming-duckstation, id: duckstation, category: emulator }
  - { desktop_id: gaming-pcsx2, id: pcsx2, category: emulator }
  - { desktop_id: gaming-melonds, id: melonds, category: emulator }
  - { desktop_id: gaming-ppsspp, id: ppsspp, category: emulator }
  - { desktop_id: gaming-rpcs3, id: rpcs3, category: emulator }
  - { desktop_id: gaming-xemu, id: xemu, category: emulator }
  - { desktop_id: gaming-eden, id: eden, category: emulator }
  - { desktop_id: gaming-cemu, id: cemu, category: emulator }
  - { desktop_id: gaming-vita3k, id: vita3k, category: emulator }
  - { desktop_id: gaming-retroarch, id: retroarch, category: emulator }
  - { desktop_id: gaming-flycast, id: flycast, category: emulator }
  # azahar.yml header records the upstream in a comment, not a dg_* var.
  - { desktop_id: gaming-azahar, id: azahar, category: emulator,
      github: "azahar-emu/azahar" }
  - desktop_id: gaming-shadps4-gui
    id: shadps4-gui
    category: emulator
    github: "{{ dg_shadps4_qtlauncher_releases_api | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"

  # ── Companion tools ──────────────────────────────────────────────
  - desktop_id: gaming-xenia-manager
    id: xenia-manager
    category: tool
    github: "{{ dg_xenia_manager_release_api | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  - desktop_id: gaming-eden-cheats-manager
    id: eden-cheats-manager
    category: tool
    github: "{{ dg_eden_cheats_manager_download_url | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  - desktop_id: gaming-7th-heaven
    id: 7th-heaven
    category: tool
    github: "{{ dg_7th_heaven_release_url | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  # Launches FFVII classic through 7th Heaven — a Wine game, not a tool.
  - desktop_id: gaming-ffvii-7th-heaven
    id: ffvii-7th-heaven
    category: wine
    github: "{{ dg_7th_heaven_release_url | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"

  # ── Ports / source reimplementations ─────────────────────────────
  - desktop_id: gaming-ship-of-harkinian
    id: ship-of-harkinian
    category: port
    github: "{{ dg_ship_of_harkinian_asset_url | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  - desktop_id: gaming-2ship2harkinian
    id: 2ship2harkinian
    category: port
    github: "{{ dg_two_ship2harkinian_asset_url | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  - desktop_id: gaming-starship
    id: starship
    category: port
    github: "{{ dg_starship_asset_url | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  - desktop_id: gaming-spaghettikart
    id: spaghettikart
    category: port
    github: "{{ dg_spaghettikart_asset_url | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  - desktop_id: gaming-cannonball
    id: cannonball
    category: port
    github: "{{ dg_cannonball_repo | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  - desktop_id: gaming-wipeout-pe
    id: wipeout-pe
    category: port
    github: "{{ dg_wipeout_pe_asset_url | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  - desktop_id: gaming-perfect-dark
    id: perfect-dark
    category: port
    github: "{{ dg_pd_repo }}"
  - desktop_id: gaming-dude
    id: dude
    category: port
    github: "{{ dg_dude_repo | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  - desktop_id: gaming-dusk
    id: dusk
    category: port
    github: "{{ dg_dusk_release_api | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  - desktop_id: gaming-doom2-ray-traced
    id: doom2-ray-traced
    category: port
    github: "{{ dg_prboom_rt_release_url | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  # SDL source port of the SNES original (UltraStarFox codebase), not a
  # decomp/recomp — see starfox_enhanced.yml.
  - desktop_id: gaming-starfox-enhanced
    id: starfox-enhanced
    category: port
    github: "{{ dg_sfe_repo }}"
  # Fan-made enhanced port of Sonic 3 & Knuckles (needs the original ROM).
  - desktop_id: gaming-sonic3air
    id: sonic3air
    category: port
    github: "{{ dg_s3air_repo }}"

  # ── Decompilations ───────────────────────────────────────────────
  - desktop_id: gaming-render96ex
    id: render96ex
    category: decomp
    github: "{{ dg_render96ex_repo_url | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  - desktop_id: gaming-sonic2-2013
    id: sonic2-2013
    category: decomp
    github: "{{ dg_s2013_repo | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"

  # ── Static recompilations ────────────────────────────────────────
  - desktop_id: gaming-dk64-recomp
    id: dk64-recomp
    category: recomp
    github: "{{ dg_dk64_repo }}"
  - desktop_id: gaming-dkc1-recomp
    id: dkc1-recomp
    name: "{{ dg_dkc_games | selectattr('key', 'eq', 'dkc1') | map(attribute='name') | first }}"
    category: recomp
    github: "{{ dg_dkc_games | selectattr('key', 'eq', 'dkc1') | map(attribute='repo') | first | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  - desktop_id: gaming-dkc2-recomp
    id: dkc2-recomp
    name: "{{ dg_dkc_games | selectattr('key', 'eq', 'dkc2') | map(attribute='name') | first }}"
    category: recomp
    github: "{{ dg_dkc_games | selectattr('key', 'eq', 'dkc2') | map(attribute='repo') | first | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  - desktop_id: gaming-dkc3-recomp
    id: dkc3-recomp
    name: "{{ dg_dkc_games | selectattr('key', 'eq', 'dkc3') | map(attribute='name') | first }}"
    category: recomp
    github: "{{ dg_dkc_games | selectattr('key', 'eq', 'dkc3') | map(attribute='repo') | first | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  - desktop_id: gaming-banjo-recomp
    id: banjo-recomp
    category: recomp
    github: "{{ dg_banjo_repo }}"
  - desktop_id: gaming-goldeneye
    id: goldeneye
    category: recomp
    github: "{{ dg_ge64_repo | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  - desktop_id: gaming-mph-recomp
    id: mph-recomp
    category: recomp
    github: "{{ dg_mph_repo }}"
  - desktop_id: gaming-waverace-recomp
    id: waverace-recomp
    category: recomp
    github: "{{ dg_wr64_zip_url | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  - desktop_id: gaming-unleashed-recomp
    id: unleashed-recomp
    category: recomp
    github: "{{ dg_unleashed_recomp_release_api | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  - desktop_id: gaming-triaevum
    id: triaevum
    category: recomp
    github: "{{ dg_triaevum_zip_url | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"

  # ── Fan games / fan remakes ──────────────────────────────────────
  - desktop_id: gaming-smb-remastered
    id: smb-remastered
    category: fangame
    github: "{{ dg_smb_remastered_repo }}"
  - desktop_id: gaming-smw-remastered
    id: smw-remastered
    category: fangame
    github: "{{ dg_smw_remastered_repo }}"
  - desktop_id: gaming-gdiffuser
    id: gdiffuser
    category: fangame
    github: "{{ dg_gdiffuser_repo }}"
  # itch.io / GameJolt / blog-distributed fan games — no GitHub repo recorded
  # anywhere in the repo, so no github key.
  - { desktop_id: gaming-mmx-regenesis, id: mmx-regenesis, category: fangame }
  - { desktop_id: gaming-dklr, id: dklr, category: fangame }
  - { desktop_id: gaming-project-reignition, id: project-reignition, category: fangame }
  - { desktop_id: gaming-sonic-p06, id: sonic-p06, category: fangame }
  - { desktop_id: gaming-sonic-sms-remake, id: sonic-sms-remake, category: fangame }
  - { desktop_id: gaming-openbor-sor-troubles-in-japan, id: openbor-sor-troubles-in-japan, category: fangame }
  - { desktop_id: streets-of-rage-remake, id: streets-of-rage-remake, category: fangame }

  # ── Windows games under Wine ─────────────────────────────────────
  # Names come from dg_pc_racing_games; the rendered desktop Name= carries a
  # "(PC on gaming)" suffix.
  - desktop_id: pc-colin-mcrae-rally-1
    id: colin-mcrae-rally-1
    name: "{{ dg_pc_racing_games | selectattr('slug', 'eq', 'colin-mcrae-rally-1') | map(attribute='name') | first }}"
    category: wine
  - desktop_id: pc-colin-mcrae-rally-2
    id: colin-mcrae-rally-2
    name: "{{ dg_pc_racing_games | selectattr('slug', 'eq', 'colin-mcrae-rally-2') | map(attribute='name') | first }}"
    category: wine
  - desktop_id: pc-colin-mcrae-rally-3
    id: colin-mcrae-rally-3
    name: "{{ dg_pc_racing_games | selectattr('slug', 'eq', 'colin-mcrae-rally-3') | map(attribute='name') | first }}"
    category: wine
  - desktop_id: pc-colin-mcrae-rally-04
    id: colin-mcrae-rally-04
    name: "{{ dg_pc_racing_games | selectattr('slug', 'eq', 'colin-mcrae-rally-04') | map(attribute='name') | first }}"
    category: wine
  - desktop_id: pc-colin-mcrae-dirt
    id: colin-mcrae-dirt
    name: "{{ dg_pc_racing_games | selectattr('slug', 'eq', 'colin-mcrae-dirt') | map(attribute='name') | first }}"
    category: wine
  - desktop_id: pc-colin-mcrae-dirt-2
    id: colin-mcrae-dirt-2
    name: "{{ dg_pc_racing_games | selectattr('slug', 'eq', 'colin-mcrae-dirt-2') | map(attribute='name') | first }}"
    category: wine
  - desktop_id: pc-outrun-2006
    id: outrun-2006
    name: "{{ dg_pc_racing_games | selectattr('slug', 'eq', 'outrun-2006') | map(attribute='name') | first }}"
    category: wine
  - desktop_id: pc-prince-of-persia-sot
    id: prince-of-persia-sot
    name: "{{ dg_pc_racing_games | selectattr('slug', 'eq', 'prince-of-persia-sot') | map(attribute='name') | first }}"
    category: wine
  - desktop_id: pc-richard-burns-rally
    id: richard-burns-rally
    name: "{{ dg_pc_racing_games | selectattr('slug', 'eq', 'richard-burns-rally') | map(attribute='name') | first }}"
    category: wine
  - desktop_id: pc-sega-rally-revo
    id: sega-rally-revo
    name: "{{ dg_pc_racing_games | selectattr('slug', 'eq', 'sega-rally-revo') | map(attribute='name') | first }}"
    category: wine
  # Wanszai arcade-to-PC wrappers under Wine (ridge_racer.yml / sega_rally.yml
  # / model1.yml).
  - desktop_id: ridge-racer
    id: ridge-racer
    name: "Ridge Racer Collection"
    category: wine
    github: "{{ dg_ridge_racer_url | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  - desktop_id: sega-rally-hd
    id: sega-rally-hd
    category: wine
    github: "{{ dg_sega_rally_url | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  - desktop_id: virtua-racing
    id: virtua-racing
    category: wine
    github: "{{ dg_model1_vr_url | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"
  - desktop_id: virtua-fighter
    id: virtua-fighter
    category: wine
    github: "{{ dg_model1_vf_url | regex_replace('^https://(api\\.)?github\\.com/(repos/)?([^/]+/[^/]+?)(\\.git)?(/.*)?$', '\\3') }}"

  # ── MS-DOS games under dosbox-staging ────────────────────────────
  # No Wine, no port — ogm's catch-all. Names from dg_screamer_games.
  - desktop_id: screamer
    id: screamer
    name: "{{ dg_screamer_games | selectattr('slug', 'eq', 'screamer') | map(attribute='name') | first }}"
    category: custom
  - desktop_id: screamer2
    id: screamer2
    name: "{{ dg_screamer_games | selectattr('slug', 'eq', 'screamer2') | map(attribute='name') | first }}"
    category: custom
  - desktop_id: screamer-rally
    id: screamer-rally
    name: "{{ dg_screamer_games | selectattr('slug', 'eq', 'screamer-rally') | map(attribute='name') | first }}"
    category: custom
